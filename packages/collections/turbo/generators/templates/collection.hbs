import { CollectionConfig } from "payload";

export const {{ name }}Collection: CollectionConfig = {
    slug: "{{ name }}",
    auth: true,
    admin: {
    },
    access: {
        read: () => true,
        create: () => true,
        update: () => true,
        delete: () => true,
    },
    fields: [],
};
